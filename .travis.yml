language: python

python:

  - "3.6"

services:
  - postgresql

env:
  - DATABASE_URL="dbname=fastfoodfasttests user=emmanuelbeja password=#1Emmcodes"
  - SECRET_KEY="3d6e1695ba974064b7d6c2ef2ebc39e3"

install:
  - pip3 install -r requirements.txt

before_script:
  - psql -c 'create database fastfoodfasttests;' -U postgres
  - psql -c "CREATE USER emmanuelbeja WITH PASSWORD '#1Emmcodes';" -U postgres

script: pytest --cov=app/tests/

after_success: coveralls
